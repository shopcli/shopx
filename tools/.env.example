# OpenRouter API Configuration
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Override default address
# DELIVERY_ADDRESS=Engineering 7, 200 University Ave W, Waterloo, ON N2L 3G5

# Optional: Run in headless mode (true/false)
# HEADLESS=true

# Optional: Page timeout in milliseconds
# TIMEOUT=30000

# Cookie-based authentication (recommended)
# The agent will automatically load saved cookies from cookies/ubereats-cookies.json
# To set up: log into UberEats manually, then run the test-auth script